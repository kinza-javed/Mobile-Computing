name: flutter_application_1
description: A new Flutter project

publish_to: 'none' # Remove this line if you are planning to publish
version: 1.0.0+1

environment:
  sdk: ">=2.18.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  intl: ^0.18.1
  
  uuid: ^3.0.6  # Add the uuid package here

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.1  # Add the flutter_lints package here

flutter:
  uses-material-design: true
